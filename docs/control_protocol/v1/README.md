# Control protocol schema bundle v1

`common.schema.json` defines shared identifiers, state, errors, and artifacts.
`requests.schema.json` and `responses.schema.json` define the eight operation
messages.  The bundle is design data only and is not installed as a runtime
package asset in this phase.
